<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.ketie.app.android" >
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>
    <application
        android:name=".KApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".LauncherActivity"  android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name="me.ketie.app.android.auth.AuthActivity" android:launchMode="singleInstance"/>


        <!--weixin auth2.0 callback-->
        <activity android:name=".auth.weixin.WXEntryActivity" android:theme="@style/LoginDialog"/>
        <activity-alias
            android:exported="true"
            android:targetActivity=".auth.weixin.WXEntryActivity"
            android:name=".wxapi.WXEntryActivity">
        </activity-alias>
        <meta-data android:value="55191195fd98c5ceb700000c" android:name="UMENG_APPKEY"/>
        <meta-data android:value="${CHANNEL_NAME}" android:name="UMENG_CHANNEL"/>
    </application>
</manifest>

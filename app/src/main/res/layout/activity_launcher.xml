<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--food-->
    <RadioGroup
        android:id="@+id/food_container"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/btn_home"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="#000000"
            android:drawableTop="@drawable/ic_home"
            android:button="@null"/>

        <RadioButton
            android:id="@+id/btn_creation"
            android:layout_width="match_parent"
            android:background="#000000"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="创作" />

        <RadioButton
            android:id="@+id/btn_me"
            android:background="#000000"
            android:drawableTop="@drawable/ic_me"
            android:button="@null"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"/>
    </RadioGroup>


    <!--center content -->
    <FrameLayout
        android:id="@+id/content"
        android:layout_alignParentTop="true"
        android:layout_width="match_parent"
        android:layout_above="@id/food_container"
        android:layout_height="match_parent">
        <fragment
            android:tag="me"
            android:name="me.ketie.app.android.ui.launch.UserHomeFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
        <fragment
            android:tag="homelist"
            android:name="me.ketie.app.android.ui.launch.TimelineFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
        </FrameLayout>

</RelativeLayout>
